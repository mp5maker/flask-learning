version: "3"

services:
  backend:
    image: mp5maker/flask-understand
    restart: always
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app